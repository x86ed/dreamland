## 1. Telemetry coverage guarantee (built-in agents)

- [x] 1.1 Add a scaffold test asserting `.claude/settings.json`'s `hooks.SubagentStop` matcher is `""` (or otherwise unscoped) after `dreamland init` with "Claude Code" selected — never narrowed to a list of the ten dreamland agent names.
- [x] 1.2 Add a scaffold test asserting `.claude/settings.json`'s `hooks.PreToolUse` matcher is `Task|Agent` (the dispatch-tool name, not a target-agent allowlist).
- [x] 1.3 Run `go test ./internal/scaffold/...` and confirm both pass against the current (unmodified) `settings-patch.json` — these tests should pass today, locking in behavior that already exists rather than introducing new behavior.

## 2. --agent-name flag on coauthor and commit

- [x] 2.1 Add an `--agent-name` string flag to `cmd/coauthor.go` and `cmd/commit.go`. Not `cmd/telemetry.go` — checked during implementation planning: `telemetry write`'s collector path and `SnapshotResult` have no agent-identity concept at all, so the flag would be a no-op there; scoped out rather than added as dead weight.
- [x] 2.2 In each command's identity-resolution path, check `--agent-name` first; if non-empty, use it directly and skip `agentNameFromHookPayload()`'s stdin read entirely. If empty, fall back to the existing chain unchanged (env var → stdin `agent_type` sniff → coding-tool-name fallback).
- [x] 2.3 Add table-driven unit tests covering all ten agent names (`janus`, `phantasos`, `nyx`, `morpheus`, `phobetor`, `baku`, `iktomi`, `zhougong`, `hypnos`, `mengpo`), not just one representative example: `--agent-name <name>` produces exactly `<name>` with no stdin provided at all, for each of the ten, across both commands (`coauthor`, `commit`) — proves the stdin read is genuinely skipped for every agent, not just given a redundant right answer for one. `--agent-name` absent falls back to today's existing behavior unchanged (regression coverage, both commands).

## 3. Agent-scoped hooks (per-agent telemetry parity, Claude Code)

- [x] 3.1 Add the `hooks.Stop` frontmatter block to all ten `internal/scaffold/templates/agents/claude-code/*.md` files: `dreamland coauthor --agent-name <this-file>`, `dreamland telemetry write --tool claude-code`, `dreamland version-bump --patch`, `dreamland version-bump --minor --if-agent janus`, `dreamland commit --reason handoff --agent-name <this-file>` — all five, in that order; `--agent-name` on `coauthor`/`commit` only, matching that file's own `name:` field (`telemetry write` unchanged, no `--agent-name`). Deliberately uniform structure across all ten (including `janus.md`'s own file) — do not replicate GitHub Copilot's 4-vs-5 per-file split; see design.md Decisions.
- [x] 3.2 Add a scaffold test asserting every installed `.claude/agents/*.md` file contains the `hooks.Stop` block with all five commands in order, and that the `--agent-name` value on `coauthor` and `commit` matches that file's own `name:` (and that `telemetry write` carries no `--agent-name` argument).
- [x] 3.3 Add a test diffing each agent's frontmatter `hooks.Stop` command list against `.claude/settings.json`'s `hooks.SubagentStop` command list, asserting they match except for the added `--agent-name` argument on `coauthor`/`commit` — locks in the "declared twice, in sync" invariant so future edits to one don't silently drift from the other.

## 4. Agent-scoped hooks patch (GitHub Copilot)

- [x] 4.1 Add `--agent-name <slug>` to the `coauthor` and `commit --reason handoff` commands (not `telemetry write --tool github-copilot`) inside all ten `internal/scaffold/templates/agents/github-copilot/*.agent.md` files' existing `hooks:` blocks (`SubagentStart` and `SubagentStop`). `<slug>` is the agent's canonical lowercase name (`baku`, `hypnos`, ..., `zhougong`) — **not** a copy of that file's frontmatter `name:` field, which is a capitalized display form on Copilot (`Baku`, `Meng Po`); use the lowercase slug so `--agent-name`'s output matches what `agentNameFromHookPayload` would resolve at runtime. No other frontmatter fields change.
- [x] 4.2 Add a scaffold test asserting every installed `.github/agents/*.agent.md` file's `hooks:` commands carry `--agent-name <slug>` (lowercase canonical name, not the capitalized frontmatter `name:` value).
- [x] 4.3 Run `go test ./internal/scaffold/...` and confirm no existing GitHub Copilot scaffold test breaks (command strings changed; test assertions comparing exact command text will need updating alongside the template).

## 5. Fix dreamland commit's agent-name resolution (workspace-level path)

- [x] 5.1 In `cmd/commit.go`'s `runCommit`, when `--agent-name` is not provided, replace the bare `resolveAgentName(cfg.CodingTool)` call with the same two-step resolution `cmd/coauthor.go`'s `runCoauthor` default mode uses: `resolveAgentName(cfg.CodingTool)` then override with `agentNameFromHookPayload()` if non-empty.
- [x] 5.2 Add table-driven `cmd/commit_test.go` coverage across all ten agent names (`janus`, `phantasos`, `nyx`, `morpheus`, `phobetor`, `baku`, `iktomi`, `zhougong`, `hypnos`, `mengpo`), not just one: piping a JSON payload with `"agent_type": "<name>"` on stdin to `runCommit` (no `--agent-name` flag) produces subject `chore: <reason> checkpoint (<name>)` for each, not the coding-tool-name fallback.
- [x] 5.3 Add the same coverage for a built-in agent type: `"agent_type": "general-purpose"` on stdin produces subject `chore: <reason> checkpoint (general-purpose)` — the resolution function doesn't distinguish dreamland agents from built-ins, and neither should its test coverage.
- [x] 5.4 Add a regression test confirming the existing no-payload/timeout behavior is unchanged (subject still falls back to `resolveAgentName(cfg.CodingTool)` when no payload arrives and no `--agent-name` is passed).
- [x] 5.5 Run `go test ./cmd/...` and confirm all new and existing tests pass.

## 6. Coding-tool co-author trailer

- [x] 6.1 In `cmd/coauthor.go`, extract a lower-level append-and-dedupe helper out of `appendCoauthorTrailer` (name/email in, no model-ID-specific "text before first space" parsing) so it can be reused without truncating multi-word coding-tool names like `"Claude Code"`.
- [x] 6.2 Add a second trailer call using `cfg.CodingTool` verbatim (not truncated) as the name, `config.EmailClean(cfg.CodingTool) + suffix` as the email, appended immediately after the model `Co-authored-by:` line and before the `Tokens:` report line.
- [x] 6.3 Unit tests: both trailers appended in order for a fresh commit message; each trailer independently idempotent (re-running with only one already present appends just the missing one); multi-word coding-tool name (`"GitHub Copilot"`) appears in full, not truncated at the space.
- [x] 6.4 Run `go test ./cmd/...` and confirm all new and existing tests pass.
- [x] 6.5 In `resolveAgentName`, change the fallback branch (reached when no env var is set) from returning `codingTool` to returning `"janus"` when `codingTool != ""`, keeping `"dreamland"` as the final fallback when `codingTool == ""`.
- [x] 6.6 Update/add tests: no env var + `coding_tool` set → `"janus"`; no env var + `coding_tool` unset → `"dreamland"`; existing env-var-present tests unchanged.

## 7. Artifact write-guard (fixed-pipeline enforcement)

- [x] 7.1 Implement `cmd/guard_artifact.go`: reads a hook JSON payload from stdin, extracts `agent_type` and `tool_input.file_path`; compiles in the ownership table (`openspec/changes/**/{proposal,design,tasks}.md` and `openspec/**/specs/**/*.md` → `phantasos`; `internal/scaffold/templates/{agents,commands}/**` → `hypnos`; `.dreamland/reports/*.md` → `zhougong`); if the target path matches a protected pattern and `agent_type` is present but doesn't match that pattern's owner, writes a reason to stderr and exits 2; otherwise exits 0 (including when `agent_type` is absent entirely, or the path matches nothing protected — fail-open on unknown identity, fail-closed only on a known-wrong one).
- [x] 7.2 Add a `PreToolUse` entry (matcher `Write|Edit`) to `internal/scaffold/templates/hooks/bindings/claude-code/settings-patch.json` running `dreamland guard-artifact`, alongside (not replacing) the existing `PreToolUse(Task|Agent)` entry.
- [x] 7.3 Unit tests for `guard_artifact`: `phantasos` writing `openspec/changes/x/proposal.md` → allowed (exit 0); `morpheus` writing the same path → blocked (exit 2, non-empty stderr); `hypnos` writing `internal/scaffold/templates/agents/claude-code/foo.md` → allowed; any agent writing an unprotected path (e.g. a normal source file) → allowed regardless of `agent_type`; no `agent_type` in payload at all → allowed (fail-open case).
- [x] 7.4 Explicitly test the known limitation is real and documented, not silently broken by a false sense of coverage: a `Bash` tool call (not `Write`/`Edit`) is never passed to `guard_artifact` at all, since the hook only matches `Write|Edit` — confirm this with a scaffold test asserting the `PreToolUse` matcher is exactly `Write|Edit`, not something broader that would (incorrectly) suggest Bash calls are covered.
- [x] 7.5 Run `go test ./cmd/... ./internal/scaffold/...` and confirm all new and existing tests pass.

## 8. Routing guardrails: Janus validation (Claude Code) + Iktomi→Phobetor (both platforms)

- [x] 8.1 Edit `internal/scaffold/templates/agents/claude-code/janus.md`: add explicit instruction language directing Janus to check any hand-off context it receives (entry dispatch, ambiguous/terminal reports, broad-routing agents' reports) against the known three-tier graph before dispatching, rather than complying with an out-of-graph suggestion. Prose-only change, no frontmatter fields.
- [x] 8.2 Edit `internal/scaffold/templates/agents/claude-code/iktomi.md`: replace step 3 ("Otherwise, report completion or blockers to Janus when done") with a split, mirroring Morpheus's phrasing pattern (`morpheus.agent.md`: "Once implementation is complete, hand off directly to phobetor for validation — a fixed next step, do not report to Janus first."): if the work included file changes, hand off directly to `phobetor` for validation as a fixed next step; otherwise report to Janus as before.
- [x] 8.3 Edit `internal/scaffold/templates/agents/github-copilot/iktomi.agent.md`: same split as 8.2, same Morpheus-mirrored phrasing, in Copilot's prose style. No frontmatter change — `agents:` already lists `phobetor`.
- [x] 8.4 Add scaffold tests asserting the installed `janus.md` (Claude Code) and `iktomi.md`/`iktomi.agent.md` (both platforms) contain the new instruction text (string-presence checks — this is prompt content, not behavior a unit test can execute).
- [x] 8.5 Run `go test ./internal/scaffold/...` and confirm all new and existing tests pass.

## 9. Repo self-hosting / dogfooding

- [x] 9.1 Run the built `dreamland` binary's `init` command (Claude Code selected) against a disposable worktree or `--force` against this repo's working tree, and capture the full diff of everything it writes or changes (`.claude/agents/`, `.claude/commands/drmlnd/`, `.claude/settings.json`). Done against a disposable copy (`scaffold.Install` called directly, bypassing the interactive `huh` wizard which needs a TTY): all ten `.claude/agents/*.md` installed with correct `hooks.Stop` blocks; `.claude/settings.json` merged correctly, including the new `guard-artifact` `PreToolUse(Write|Edit)` entry alongside the existing `Task|Agent` one; `.claude/commands/drmlnd/*` left untouched (already present, correctly skipped).
- [x] 9.2 Diff the generated output against what `agent-scaffolding`, `dev-workflow-hooks`, `otel-tool-config`, `router-slash-commands`, `agent-telemetry-coverage`, `fixed-pipeline-enforcement`, `janus-dispatch-guardrails`, and `janus-router-agent` specs require; file and fix any drift found as part of this change. No drift found — output matched every spec requirement inspected (hook order, `--agent-name` values, unscoped matchers, guard wiring).
- [x] 9.3 Reconcile this repo's own hand-maintained `.claude/settings.json` (currently only a `Stop` → `scripts/pre-merge-check.sh` hook) with the dreamland-generated hooks, preserving the existing pre-merge-check hook. Confirmed via the dogfood run: `deepMerge`'s array-union kept the existing `pre-merge-check.sh` `Stop` entry as its own array element, untouched, alongside dreamland's own `Stop` entry.
- [ ] 9.4 Commit the merged `.claude/agents/` and updated `.claude/settings.json` to this repo, making dreamland self-hosted — this is the default outcome of a successful dogfood run, not an open question. Get explicit user confirmation immediately before that commit, since it changes tracked repo state; do not commit silently as a side effect of running `dreamland init`. **Blocked on user confirmation — not yet done.**

## 10. Live verification with Claude Code

- [ ] 10.1 Start a real Claude Code session in the scaffolded repo and confirm `/drmlnd:route` (and at least one `/drmlnd:<agent>` direct command) dispatches to the correct dreamland agent.
- [ ] 10.2 Deliberately provoke a built-in-agent dispatch (a request shaped like open-ended exploration or a task matching `general-purpose`'s or `Explore`'s description) and confirm it completes normally — not refused — and inspect the resulting commit for both `Co-authored-by:` trailers, a `Tokens:` line, and an agent-attributed subject (`chore: ... checkpoint (general-purpose)` or `(Explore)`).
- [ ] 10.3 Complete at least one real dreamland-agent hand-off and inspect the resulting commit: confirm the author name and the `chore: handoff checkpoint (<agent-name>)` subject agree, and that both `Co-authored-by:` trailers (model, coding tool) and a `Tokens:` line are present in the commit body.
- [ ] 10.4 Compare the dreamland-agent commit (10.3) and the built-in-agent commit (10.2) side by side: confirm both have the same trailer keys and subject shape, differing only in the agent-name/token values.
- [ ] 10.5 Deliberately have a non-`phantasos` agent (e.g. `morpheus`, mid-task) attempt to write to an `openspec/changes/**/proposal.md`-shaped path and confirm Claude Code refuses the write with the guard's stderr reason surfaced. Then confirm `phantasos` performing the same write succeeds normally.
- [ ] 10.6 Confirm a main-thread-direct edit to a protected path (no subagent dispatched, just asking the top-level assistant to edit `openspec/changes/x/proposal.md` directly) is **not** blocked — this is the accepted, documented coverage gap, not a bug; verifying it behaves as designed (unblocked) rather than unexpectedly (blocked or crashing).
- [ ] 10.7 Compare a Claude Code commit against a real (or previously captured) GitHub Copilot dreamland commit and confirm both are diffable/greppable the same way (`git log --format='%an %s%n%b'`), including that both carry a coding-tool `Co-authored-by:` line naming their respective platform.
- [ ] 10.8 Using at least 3 ordered commits from the above, manually compute per-agent token delta by subtracting each commit's cumulative `Tokens:` total from the previous commit's, and confirm the result matches that agent's actual turn cost — proves the chosen tuning mechanism (delta-by-subtraction over git log, no telemetry schema change) actually works end to end.
- [ ] 10.9 Deliberately provoke a free-form Iktomi task that makes a file change, and confirm it hands off to Phobetor rather than reporting straight to Janus; separately provoke a purely investigative Iktomi task and confirm it still reports to Janus directly.
- [ ] 10.10 Record the verification steps and outcome (pass/fail, Claude Code version used) in the change's notes for reviewers, since this can't be fully automated in CI.
