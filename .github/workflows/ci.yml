name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build & Vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - run: go build ./...
      - run: go vet ./...

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Check all packages have test files
        run: bash scripts/pre-merge-check.sh find_untested_packages
        env:
          MERGE_CHECK: "1"
      - name: Run tests
        run: bash scripts/pre-merge-check.sh run_tests
        env:
          MERGE_CHECK: "1"
          COVERAGE_OUT: coverage.out
      - uses: actions/upload-artifact@v4
        with:
          name: coverage-profile
          path: coverage.out
          retention-days: 1

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/download-artifact@v4
        with:
          name: coverage-profile
      - name: Check coverage thresholds
        run: bash scripts/pre-merge-check.sh check_coverage
        env:
          MERGE_CHECK: "1"
          COVERAGE_PROFILE: coverage.out

  godoc:
    name: Godoc
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Check exported symbols have godoc
        run: bash scripts/pre-merge-check.sh check_godoc

  version:
    name: Version Bump
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Check version is bumped
        run: bash scripts/pre-merge-check.sh check_version_bump
        env:
          MERGE_CHECK: "1"
