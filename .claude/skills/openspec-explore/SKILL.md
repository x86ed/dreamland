---
name: openspec-explore
description: Legacy alias for /opsx:explore — enter explore mode, a thinking partner for exploring ideas, investigating problems, and clarifying requirements. Use when the user wants to think through something before or during a change.
license: MIT
compatibility: Requires openspec CLI.
metadata:
  author: openspec
  version: "1.0"
  generatedBy: "1.3.1"
---

This is a legacy name for `/opsx:explore`, kept so old invocations still resolve. It routes to the same target — the `phantasos` agent — directly, not through Janus.

Invoke the `opsx:explore` skill with the same input and follow its instructions; do not duplicate its steps here.
