Dispatch notes. Go code (`cmd/`, `internal/routing/`, `internal/oneiroi/`, `internal/scaffold/*.go`, `internal/config/`) is `nyx` (tests first) then `morpheus`. Anything under `internal/scaffold/templates/agents/**` or `.../commands/**` is writable only by `hypnos` (fixed artifact ownership), so those tasks are tagged `hypnos` even where they are prose edits. Each task line ends with the `[flow: ...]` tag this change introduces (see the `deterministic-routing` spec), so this file also exercises the convention.

## 0. Live verification of platform assumptions (gate for groups 2, 3, 5)

Run against a real Claude Code session in a scratch repo with a build of `dreamland` from this branch. Record observed payloads and results under this task in the commit message of the task that follows; if an assumption fails, stop and return to `phantasos` to amend design.md (do not work around it silently).

- [ ] 0.1 Add a temporary `PreToolUse` hook that dumps stdin to a scratch file, then observe: (a) under `claude --agent janus`, does the payload carry top-level `agent_type: "janus"`; (b) under plain `claude`, is `agent_type` absent; (c) does a frontmatter `hooks.PreToolUse` entry in `janus.md` fire under `--agent janus`. Outcome decides which of the two guard wirings is load-bearing (design.md, Risks). [flow: morpheus]
- [ ] 0.2 With `tools: Read, Bash, Agent(morpheus, nyx)` on a scratch agent run as `--agent`, confirm it can spawn `morpheus`, cannot spawn `general-purpose`, and that `Agent` appears as the `tool_name` in `PreToolUse`. [flow: morpheus]
- [ ] 0.3 Confirm the `Bash` `PreToolUse` payload shape: `tool_name` `Bash`, command at `tool_input.command`; and whether a `PowerShell` tool exists on the installed version and its `tool_name`/`tool_input` field. `guard-router` field names in group 2 are taken from this observation. [flow: morpheus]
- [ ] 0.4 Try `!`-prefixed inline command execution (with `allowed-tools: Bash(dreamland route:*)`) in a scratch slash command with no interpolated user text; confirm the output is in the model's prompt before its first turn. Adopt in group 5 only if it works on the installed version. [flow: morpheus]
- [ ] 0.5 Baseline: run the same three requests (free-form, `/opsx:apply` on a one-change repo, `/drmlnd:morpheus`) on the pre-change build and record `dreamland telemetry` token totals and number of subagent turns, so group 10 can compare after. [flow: morpheus]

## 1. `dreamland route`

- [ ] 1.1 Write `internal/routing/routing_test.go` covering every scenario in the `deterministic-routing` spec against a fake `Env` (no real `openspec`): rules 1-6 in order, state resolver outcomes, tag parsing, unregistered `--to`/tag exits with an error, `janus` rejected, seeded agent accepted, roster populated on ambiguity, rule ids stable. [flow: nyx]
- [ ] 1.2 Create `internal/routing/routing.go`: `type Input struct{ To, Command, Change, Request string }`, `type Result struct` with the JSON fields in the spec (`decision`, `target`, `rule`, `reason`, `change`, `task`, `candidates`, `ask_user`, `roster`, `next`), `type Env interface{ ListChanges() ([]Change, error); ChangeStatus(name string) (Status, error); ReadTasks(name string) (string, error); Registered() map[string]bool; Roster() []RosterEntry }`, and `func Resolve(in Input, env Env) (Result, error)` implementing rules 1-6 and the state resolver. Intent phrases live in one package-level table. Tag parsing is `\[flow:\s*(nyx|morpheus|hypnos|mengpo)\]` on the first unchecked `- [ ]` line. [flow: morpheus]
- [ ] 1.3 Create `cmd/route.go` (`routeCmd`, flags `--to`, `--command`, `--change`, `--stdin`; trailing args or stdin as request): builds an `Env` backed by `exec.LookPath("openspec")` + `openspec list --json` / `openspec status --change <n> --json` (no shell), reads `openspec/changes/<name>/tasks.md` with `filepath`, `agentidentity.Registered(repoRoot)` for targets and `.dreamland/oneiroi/registry.json` roles for `roster`; prints one JSON object; errors return a plain error (exit 1), never `Blocking`. Writes no files. [flow: morpheus]
- [ ] 1.4 Add `cmd/route_test.go` asserting no side effects (`git status --porcelain` and `.dreamland-session.json` unchanged), exit 1 when `openspec` is missing, and stdin request literalness. Add a Windows build/test in the style of `cmd/otel_receiver_windows_build_test.go` asserting backslash paths resolve. [flow: nyx]
- [ ] 1.5 Add the drift test from the `deterministic-routing` spec (rule targets and `--command` spellings versus the routing table in every platform's `janus.*` template; flow-tag set versus the `/opsx:apply` entry). It fails until group 5 lands the templates; keep it in the tree red-then-green. [flow: nyx]

## 2. `dreamland guard-router`

- [ ] 2.1 Write `cmd/guard_router_test.go` from every scenario in the `router-dispatch-guard` spec, table-driven for the Bash allowlist (allowed forms, each blocked metacharacter, `--output=`, `-o`, `--ext-diff`, quoted-heredoc allowed/continued-after-terminator/on-another-command, `PowerShell` always blocked) and for identity resolution (flag beats payload, payload beats unattributed, git config never consulted: set `user.name` to `janus` and assert no effect). Stub exit via a `guardRouterExit` variable as `guard_artifact.go` does. [flow: nyx]
- [ ] 2.2 Add `MainThreadGuard string \`json:"main_thread_guard,omitempty"\`` to `internal/config/config.go` `Config`; accept only `""`, `"allow"`, `"block"` (anything else loads as `allow` with a stderr note). Test in `internal/config/config_test.go`. [flow: nyx]
- [ ] 2.3 Create `cmd/guard_router.go`: `guardRouterCmd` (`--agent-name`), `resolveActingIdentity(flag string, payload hookToolInputPayload) string`, `janusBashAllowed(command string) (ok bool, reason string)`, and the blocking message builder (reuse `artifactOwners` from `cmd/guard_artifact.go` to name the owner for protected paths; otherwise point at `dreamland route` and name `iktomi`). Deny via `Blocking(...)` so `Execute()` exits 2; empty/malformed payload returns nil. Field names per task 0.3. [flow: morpheus]
- [ ] 2.4 Extend the payload struct (or add one) to include `tool_name` and `tool_input.command`; keep `guard-artifact` behavior byte-identical (its tests must still pass unchanged). [flow: morpheus]

## 3. Scaffold plumbing (Go)

- [ ] 3.1 Write `internal/scaffold/scaffold_test.go` cases: rendered Claude Code `janus.md` has `tools: Read, Bash, Agent(<nine built-ins, then registry entries in order>)` and no `janus`; re-render with a seeded registry entry keeps it; `settings.json` merge yields `guard-router` exactly once and preserves user `PreToolUse` entries; no top-level `"agent"` key; `permissions.allow` contains `Bash(dreamland route *)` and is not duplicated on repeat init. [flow: nyx]
- [ ] 3.2 In `internal/scaffold/scaffold.go`, render a `{{DISPATCH_TARGETS}}` placeholder in the Claude Code Janus template from `agentidentity.Registered(cfg.RepoRoot)` (built-ins in fixed order, then registry order, never `janus`). Non-Claude templates are not rendered. [flow: morpheus]
- [ ] 3.3 Extend the `settings.json` merge to union-merge `permissions.allow` from the patch (array union, no duplicates) if `mergeJSON` currently overwrites arrays; add `"permissions": {"allow": ["Bash(dreamland route *)"]}` handling accordingly. [flow: morpheus]

## 4. Roster coherence

- [ ] 4.1 Write tests in `internal/oneiroi/routing_test.go`: `seed`/`fork` add the new name to the installed `janus.md` `Agent(...)` list in registry order, are idempotent, never add `janus`, and no-op when the file is absent; a helper `RemoveDispatchTarget` removes a name and is idempotent. [flow: nyx]
- [ ] 4.2 In `internal/oneiroi/routing.go` add `AddDispatchTarget(repoRoot, agentName string) (touched bool, err error)` and `RemoveDispatchTarget(repoRoot, agentName string) (touched bool, err error)` operating on the `Agent(...)` group in the installed Claude Code Janus file's `tools:` line; call `AddDispatchTarget` from the same place `AddStubEdges` is called in `cmd/oneiroi.go` (seed/fork), and include the file in that command's commit path list. [flow: morpheus]
- [ ] 4.3 Add the registry-versus-`Agent(...)` drift test from the `agent-lifecycle-management` spec (fails when a registered name, excluding `janus`, is missing from the list or the list contains an unregistered name). [flow: nyx]
- [ ] 4.4 Check whether the existing routing-table stub edge survives a forced re-init; record the answer in design.md's Decision 6. If it does not, open a follow-up rather than widening this change. [flow: morpheus]
- [ ] 4.5 Update `internal/scaffold/templates/agents/claude-code/mengpo.md`'s archive and hard-delete steps to also remove the agent's name from the `Agent(...)` list in the installed `.claude/agents/janus.md` (`mengpo` has `Write`; there is no `oneiroi` retire subcommand today and this task does not add one). The registry-versus-`Agent(...)` drift test (4.3) is the backstop if the step is missed. `RemoveDispatchTarget` (4.2) is provided for tests and any future command. [flow: hypnos]

## 5. Templates and command files (`hypnos`)

- [ ] 5.1 `internal/scaffold/templates/agents/claude-code/janus.md`: set `tools: Read, Bash, Agent({{DISPATCH_TARGETS}})`; replace the `hooks.Stop` block with `hooks.PreToolUse` (matcher `Write|Edit|MultiEdit|NotebookEdit|Bash|PowerShell`, command `dreamland guard-router --agent-name janus`); rewrite the opening protocol to at most one short paragraph deferring to `dreamland route` (run it with the request, dispatch `target`, decide among `candidates` or ask the user when `ask_user`, never do the work); keep the routing table, the hand-off validation paragraph from `claude-code-parity`'s `janus-dispatch-guardrails`, and the verbatim-forwarding rule; add `dreamland route` and the `[flow: ...]` task tag names (including legacy `openspec-*` spellings) to the table so the drift test passes; state that free text is passed with `dreamland route --stdin` in a quoted heredoc with delimiter `DREAMLAND_ROUTE_EOF`. [flow: hypnos]
- [ ] 5.2 `internal/scaffold/templates/commands/claude-code/route.md`: replace "Delegate this request to the `janus` agent" with instructions to run `dreamland route` (free text via quoted heredoc on `--stdin`; inline pre-exec form only if task 0.4 passed) and dispatch the returned `target` with the `Agent` tool, forwarding the request verbatim; on `ambiguous`, choose among `candidates` or ask the user if `ask_user`. Keep the `<!-- dreamland-managed -->` marker handling identical (bare `dreamland.md`/`janus.md` copies derive from this file). [flow: hypnos]
- [ ] 5.3 `internal/scaffold/templates/commands/claude-code/{baku,hypnos,iktomi,mengpo,morpheus,nyx,phantasos,phobetor,zhougong}.md`: replace the "delegate to the `janus` agent with an explicit instruction" text with "dispatch `<agent>` directly with the `Agent` tool, forwarding the request verbatim; do not spawn `janus`". Update the generator in `internal/scaffold/stub.go` (line ~226, `body := fmt.Sprintf("Delegate this request to the `janus` agent ...`) for Claude Code so `dreamland oneiroi seed`/`fork` produce the same text. [flow: hypnos]
- [ ] 5.4 `internal/scaffold/templates/hooks/bindings/claude-code/settings-patch.json`: add a `PreToolUse` entry, matcher `Write|Edit|MultiEdit|NotebookEdit|Bash|PowerShell`, command `dreamland guard-router`; add `permissions.allow` `Bash(dreamland route *)`. Leave every other entry unchanged. [flow: hypnos]
- [ ] 5.5 `phantasos.*` in all six platform template trees: add one instruction that every checkbox task line in `tasks.md` ends with exactly one of `[flow: nyx]`, `[flow: morpheus]`, `[flow: hypnos]`, `[flow: mengpo]`, with the one-line meaning of each. [flow: hypnos]
- [ ] 5.6 Update the `/opsx:apply` command template (the generated `.claude/commands/opsx/apply.md` "Routing" line) and the `openspec-apply-change` stub to describe `dreamland route --command opsx:apply` for Claude Code. If these files are upstream-generated by OpenSpec rather than dreamland templates, record where they come from and make the change in the dreamland-owned source instead of the generated copy. [flow: hypnos]
- [ ] 5.7 Update `README.md` (routing section, the Janus and Hypnos rows of the agent table, and a short "launch modes" note: `claude --agent janus` vs `/dreamland`, `main_thread_guard`). [flow: morpheus]

## 6. Wiring tests

- [ ] 6.1 Extend the scaffold tests to assert, per platform, that only Claude Code's installed bindings reference `guard-router`, and that Claude Code's `janus.md` has no `hooks.Stop` while the other nine agents' blocks are byte-identical to their pre-change templates. [flow: nyx]
- [ ] 6.2 Add an end-to-end test (build the binary, feed recorded hook payloads) exercising `guard-router` under `janus` for Edit, Write, allowed Bash, blocked Bash, and `Agent`, asserting exit codes 2/2/0/2/0. [flow: nyx]

## 7. Live verification (after groups 1-6)

- [ ] 7.1 In a scratch repo: `claude --agent janus`, ask for a code change; confirm Janus runs `dreamland route`, dispatches `iktomi`, and any attempted `sed -i` or `Edit` by Janus is blocked with the dispatch message. [flow: morpheus]
- [ ] 7.2 In the same repo: plain `claude`, `/dreamland <request>`; confirm no `janus` subagent appears in the transcript and the routed agent runs. Repeat for `/opsx:apply` with a tagged task and an untagged task, and for `/drmlnd:morpheus`. [flow: morpheus]
- [ ] 7.3 Two parallel windows in the same repo, one `--agent janus`, one plain `/dreamland`; confirm the guard blocks only the Janus window regardless of `git config user.name` contents. [flow: morpheus]
- [ ] 7.4 Repeat the 0.5 requests on the new build and record token totals and subagent-turn counts next to the baseline; if the routing saving is not visible, say so in the change's archive notes. [flow: morpheus]

## 8. Self-heal of existing installs

- [ ] 8.1 Add a staleness rule to `internal/scaffold/scaffold.go` so a plain `dreamland init` (no `Force`) rewrites (a) `.claude/agents/janus.md` when its `tools:` line lacks `Agent(`, and (b) prefixed and bare Claude Code command files still containing the sentinel text "Delegate this request to the `janus` agent", following the `installFlatCommands` `commandName` staleness precedent. A user-edited `janus.md` that already has `Agent(` is left alone. Tests for both, including an untouched already-current install. [flow: nyx]
- [ ] 8.2 Implement 8.1's rule (`isStaleJanus`, `isStaleRouteCommand`), keeping the check content-based and narrow. [flow: morpheus]

## 9. Other platforms (optional, separable; cut this group without affecting the Claude Code fix)

- [ ] 9.1 Add one line to `janus.*` for GitHub Copilot, Codex CLI, Cursor, Kiro, and Antigravity: run `dreamland route --stdin` (or with the request) first and follow `decision`/`target`; on `ambiguous`, decide among `candidates` yourself. No hook, tool, or `agents:` graph change. Confirm the drift test (1.5) covers all six trees. [flow: hypnos]

## 10. Reconcile, re-sync, archive readiness

- [ ] 10.1 After `claude-code-parity` is archived (it must archive first): amend its two now-contradicted requirements in `openspec/specs/` (the `fixed-pipeline-enforcement` scenario "Janus's own permissions are unchanged" to the new `tools` value and `"agent"`-key-absent statement, and the `agent-scaffolding` Stop-hooks-on-all-ten requirement to nine agents plus Janus's `PreToolUse` guard block) via a delta in this change's archive step, not by editing that change's files. [flow: nyx]
- [ ] 10.2 Re-sync this repository's live files from the templates through the installer (the staleness rule from group 8, or the installer's force path if run before it lands), not by hand-editing `.claude/`; confirm `.claude/settings.json` now also carries the pre-existing `guard-artifact` binding. Coordinate with `claude-code-parity` task 9.4, which is the same re-sync, so it happens once. [flow: morpheus]
- [ ] 10.3 Run `openspec validate deterministic-routing-and-janus-guard` and `MERGE_CHECK=1 bash scripts/pre-merge-check.sh`; `go build ./...`, `go vet ./...`, `go test ./...` pass on the branch. [flow: morpheus]
