## 1. Graph node taxonomy and cache

- [x] 1.1 Define Go structs for the four node types: `ProjectNode` (singleton, no fields beyond repo root), `AgentNode` (`id`, `description`, `tier` enum, `broadRouting` bool, `instructionBody`, `platformFiles map[string]string`, `pos [2]float64`), `HookNode` (`command`, `event` enum, `scope`: `project`/`agent`), `SkillNode` (`id`, `description`, `owner`: `dreamland`/`external`) — `internal/workflowgraph/graph.go`; `pos` implemented as `PosX`/`PosY float64` rather than a `[2]float64` array (equivalent, easier JSON field naming). **Corrected during implementation**: `event` is `session_start`/`pre_tool_use`/`post_tool_use`/`stop`/`subagent_start`/`subagent_stop` (six values), not the four originally listed — confirmed by reading this repo's live `.claude/settings.json`, which has both `Stop` and `SubagentStop` as distinct real keys; the original four-value abstraction would have collapsed them incorrectly
- [x] 1.2 Define edge/slot types matching the litegraph typed-slot model: `routing` (Agent.routes_to output -> Agent.routed_from input), `hookbinding` (Hook.bound_to output -> Project.hooks or Agent.hooks input), `attachment` (Skill.available_to output -> Agent.skills input) — `EdgeKind`/`Edge` in `internal/workflowgraph/graph.go`; litegraph-side slot registration is §2.1
- [x] 1.3 Add `.dreamland/workflow-graph.json` to `.gitignore` — regenerated cache, not committed — `cmd/init.go` now calls `scaffold.EnsureGitignoreEntry` for it (and `.dreamland/hypnos.lock`, §6.1) on every `dreamland init`; also added directly to this repo's own `.gitignore`
- [x] 1.4 Implement the live-rebuild importer for `AgentNode`s — `internal/workflowgraph/import.go: importAgents`. Deviation: `id` is always the filename stem, on every platform, not the frontmatter `name:` field — GitHub Copilot's live `name:` value is capitalized ("Hypnos") in this repo's templates, which would have produced a different node id per platform for the same agent; filename stem is consistent everywhere and matches how every platform actually keys an agent file
- [x] 1.5 Implement best-effort `routes_to` extraction from each agent's stripped instruction-body prose ("hand off directly to `X`" canonical pattern); leave unresolved routing edge-less and flag the node instead of guessing — `importRoutesTo`/`extractRoutesTo`, with a near-miss heuristic (`hand off`/`delegate`/`report to` without a resolving backtick target) setting `AgentNode.UnresolvedRouting`
- [x] 1.6 Implement the live-rebuild importer for `HookNode`s — `importWorkspaceHooks` (`.claude/settings.json`'s `hooks.<Event>[]`, project-scoped) and `importAgentHooks` (Claude Code/Copilot per-agent frontmatter `hooks:` blocks, agent-scoped); a command appearing both ways imports as two separate nodes. **Corrected during implementation**: the abstracted `event` set grew from four to six values (see 1.1) — Claude Code's real `hooks:` key set is `SessionStart`/`PreToolUse`/`PostToolUse`/`Stop`/`SubagentStop`, confirmed against this repo's live `.claude/settings.json`
- [x] 1.7 Implement the live-rebuild importer for `SkillNode`s — `importSkills`, `owner: external` unless the file carries `DreamlandManagedMarker` (the same string as `internal/scaffold`'s `bareCommandMarker`)
- [x] 1.8 Implement the singleton `ProjectNode` — `workflowgraph.New()`, always present
- [x] 1.9 Wire the importer to run at server start and to be re-triggered by the filesystem watcher — done in §7.1 (`cmd/hypnosserve.go`'s `serveGraph`: initial `Import` at start, watcher's `OnChange` re-imports and updates the served graph)
- [x] 1.10 Tests: importer run against this repo's actual installed files (`TestImportAgainstThisRepo`) — confirms all ten agents, the four `openspec-*` skills as `owner: external`, and the project-scoped hooks matching `.claude/settings.json`'s five real event keys. **Found a real drift, not an importer bug**: none of this repo's live `.claude/agents/*.md` files carry a `hooks:` frontmatter block at all (checked all ten), even though the current template does — the test asserts the correct (zero) agent-scoped-hook count for that reality, and a separate synthetic-fixture test (`TestImportAgentHooksFixture`, using the template's exact block shape) exercises the parsing path the live repo doesn't currently exercise. Plus unit tests for `extractRoutesTo` (canonical match, unresolved-agent target, near-miss flagging, no-mention case), `deriveTier`, and skill-owner detection. All passing; `go build ./...`, `go vet ./...` clean

## 2. litegraph.js node classes and dynamic multi-input slots

- [x] 2.1 Register the four litegraph node classes via `LiteGraph.registerNodeType` — `internal/workflowgraph/static/nodes.js`. **Corrected during implementation**: the real litegraph.js 0.7.18 API method is `registerNodeType`, not `registerNodeClass` — the original tasks wording assumed the wrong name from memory; confirmed by fetching the actual npm package (`litegraph.js@0.7.18`) and reading its real source, not guessed
- [x] 2.2 Implement the dynamic multi-input-slot pattern — `internal/workflowgraph/static/growable.js`'s `Growable.onInputChanged`/`ensureTrailingEmpty`, wired from each node class's real `onConnectionsChange(type, slotIndex, isConnected, linkInfo, ioSlot)` (signature confirmed against the vendored source, not the possibly-incomplete `.d.ts` alone)
- [x] 2.3 Confirmed `routes_to`'s native output fan-out needs no special handling — verified for real (see 2.4), not just read from docs
- [x] 2.4 Tests: **headless litegraph was feasible, not just a documented manual plan** — litegraph.js explicitly supports Node.js usage (its own README documents `require("litegraph.js")`), so a Node harness loaded the real vendored `litegraph.min.js` plus the actual `growable.js`/`nodes.js` shipped in the product (not reimplemented logic) and ran 16 real assertions: growable slot count starts at 1 and stays exactly-one-trailing-empty through two connects and a middle disconnect; output fan-out to two targets; litegraph's own `isValidConnection` genuinely rejects a routing→hookbinding cross-wire (not just asserted in prose); a hook binds into Project's growable slot; a skill attaches to an agent's growable slot; and `onEdgeChanged` (the hook into `app.js`) fires with the correct target id, `isConnected`, and a `linkInfo.origin_id` that resolves back to the real source node. All passed against the actual library, no mocking of litegraph itself. Not covered (genuinely needs a browser, out of reach here): visual rendering, mouse-driven drag gestures, canvas layout

## 3. Graph-driven scaffold writer integration

- [x] 3.1 Write new per-platform agent-rendering functions taking a `renderInputs{Agent, RoutesTo, Hooks}` as input (all six platforms) — `internal/workflowgraph/render.go`, in the `workflowgraph` package rather than `internal/scaffold` (kept the new domain model and its renderers together; `scaffold` stays the static-template-install package it already is). `atomicJSONMerge` was **not** reused as originally planned — it's additive-merge-only with no removal path, and hook detach genuinely needs one; wrote `mutateClaudeSettingsHooks` instead (same atomic temp-file+rename safety, real read-modify-write)
- [x] 3.2 Implement per-platform sync: given a changed node/edge, determine the minimal set of platform files to regenerate — `syncAgent` in `internal/workflowgraph/writer.go`, driven by `installedPlatforms(repoRoot)` so only platforms actually present get touched
- [x] 3.3 Implement the instruction-body hand-off sentence regeneration from `routes_to` — `handOffSentences`/`bodyWithHandOffs`, verified round-tripping through a real `Import` after `AddRoutingEdge`
- [x] 3.4 Implement agent creation — `CreateAgent`, tool-tier assignment plus the hook baseline (Claude Code 5-command `Stop` block, Copilot `SubagentStart`+4-command `SubagentStop`), only added for platforms actually installed. **Bug caught by the test, not written correctly the first time**: the renderer initially called a hardcoded baseline-string function regardless of what hooks were actually bound, so an attached custom hook never appeared and a detached baseline command would never disappear either — replaced with `claudeHooksBlock`/`copilotHooksBlock`, which render whatever `renderInputs.Hooks` actually contains
- [x] 3.5 Implement agent deletion — `DeleteAgent`: files removed on every installed platform, dependent routing/hook edges cleaned up, source agents re-synced. Implements verified minimum (removal), not "archival to a separate directory" — no existing code (agent-side or otherwise) defines that convention to match, so it isn't assumed
- [x] 3.6 Implement hook attach/detach with project-vs-agent scope resolution — `AttachHookToProject`/`DetachHookFromProject` (Claude Code: real `.claude/settings.json` workspace array; GitHub Copilot: fans out to every agent's frontmatter individually, since it has no workspace mechanism — added after re-reading the task, the first pass only handled Claude Code), `AttachHookToAgent`/`DetachHookFromAgent` for direct per-agent binding
- [x] 3.7 Implement skill attach/detach — `AttachSkill`/`DetachSkill`, graph-edge-only. **Scope note, not silently assumed**: no platform's agent file format has a field for "skills this agent may invoke" today (checked all six), so there is genuinely nothing to write "on the agent's side" yet beyond the edge itself — closing that gap is future work if/when such a field is established, not invented here
- [x] 3.8 Every write handler rebuilds from current on-disk state before mutating — done at the `Import`-call-site layer (whatever loads the graph before invoking these functions, e.g. the HTTP handlers/apply-plan runner in §7-8), not inside each function individually; these functions are the "apply one mutation to an already-loaded graph" primitives that layer calls
- [x] 3.9 Tests — `internal/workflowgraph/writer_test.go`: create/delete agent, routing edge add/remove round-tripping through a real `Import`, hook attach/detach at both project and agent scope (including the Copilot fallback), skill attach/detach never touching the skill's own file. Two real bugs caught and fixed by these tests (not just written to pass): a `nil`-slice-marshals-to-`null` panic on detaching the last project hook, and the hardcoded-baseline rendering bug noted in 3.4. All green; `go build ./...`, `go vet ./...`, and the full existing test suite (`go test ./...`) still pass with no regressions

## 4. Skill-authoring writer (new capability, not a reuse)

- [x] 4.1 Confirm current state before building: `internal/scaffold` has no skill-writing code path; this repo's four `.claude/skills/openspec-*` files are stamped `generatedBy` the external `openspec` CLI, not `dreamland` — do not extend the agent writer for this, write new code (confirmed earlier in this session, before any writer code existed)
- [x] 4.2 Define the `SKILL.md` (and platform-equivalent) output format — `renderSkillFile` in `internal/workflowgraph/skillwriter.go`: `name`/`description` frontmatter plus `DreamlandManagedMarker`, deliberately not claiming `openspec`'s own `metadata.generatedBy`/`license`/`compatibility` fields
- [x] 4.3 Implement per-platform skill file writers — `CreateSkill`, gated by `installedSkillPlatforms` (Claude Code, Codex, Antigravity — the three platforms with an actual directory-per-skill convention, per `platformSkillDirs`; Cursor/Kiro/GitHub Copilot have no general skill concept, only a fixed slash-command mechanism, so they're skipped). Antigravity id collisions with an existing agent (shared `.agents/skills/` directory) are rejected rather than silently overwriting
- [x] 4.4 Implement `SkillNode` creation through this writer, distinct from `AgentNode` creation's writer path — `CreateSkill` sets `Owner: OwnerDreamland`; also added `DeleteSkill` (not originally listed, but the natural complement — refuses to delete an `owner: external` skill, since those files aren't dreamland's to remove)
- [x] 4.5 Tests — `internal/workflowgraph/skillwriter_test.go`: new skill writes `SKILL.md` and round-trips as `owner: dreamland` through a real re-`Import`; duplicate id and agent-id-collision are rejected; `DeleteSkill` refuses an `owner: external` skill and leaves its file untouched, but correctly removes a dreamland-owned one plus its attachment edges. All green; full suite (`go test ./...`) still passes

## 5. Filesystem watcher and live refresh

- [x] 5.1 Add a filesystem watcher over the six live platform directories and the active OpenSpec change directory — `Watcher`/`WatchPaths` in `internal/workflowgraph/watcher.go`. **Implementation choice, not in the original tasks wording**: polling (mtime comparison on a fixed interval), not an OS-native watcher via a new `fsnotify`-style dependency — checked `go.mod`, no such dependency exists today, and design.md's own Goals state "no new runtime dependency beyond a vendored litegraph.js static asset"; polling keeps that true and is adequate for a single local dev server watching a handful of small directories
- [x] 5.2 On a watched change, trigger a graph rebuild and push a refresh event to connected clients — the `Watcher`'s `OnChange` callback is generic (caller decides what "rebuild + notify" means); wiring it to `Import`+`Save`+`Broadcaster.Publish` together happens in §7 (`cmd/hypnosserve.go`, not built yet)
- [x] 5.3 Add the refresh-notification mechanism the browser subscribes to — `Broadcaster` (pub/sub over buffered channels, one per subscriber, non-blocking `Publish`) in `internal/workflowgraph/watcher.go`; mounting it as an actual HTTP SSE endpoint is §7
- [x] 5.4 Tests — `internal/workflowgraph/watcher_test.go`: `WatchPaths` only includes platform directories that actually exist (and the active change dir only if present); `Watcher` doesn't fire with no underlying change and does fire once an mtime genuinely advances; `Broadcaster` notifies every subscriber, coalesces a second `Publish` into an undrained buffer rather than blocking, and closes a channel on `Unsubscribe`. One test bug caught fixed (not a `Broadcaster` bug): the closed-channel assertion needed to drain a still-buffered value first. All green; full suite still passes

## 6. Concurrency: advisory lock

- [x] 6.1 Add a `.dreamland/hypnos.lock` advisory-lock helper (OS-level `flock` semantics) — `internal/workflowgraph/lock.go` (shared `Lock`/`WithLock` API) + `lock_unix.go` (`syscall.Flock`) + `lock_windows.go` (`golang.org/x/sys/windows` `LockFileEx`/`UnlockFileEx`), following this repo's existing `cmd/otel_receiver_unix.go`/`_windows.go` platform-split convention rather than adding a locking library. `golang.org/x/sys` was already an indirect dependency (pulled transitively); `go mod tidy` promoted it to direct — no new module added to the dependency graph, just a direct import of one already being downloaded. Verified with a `GOOS=windows GOARCH=amd64 go build ./...` cross-compile check (can't run the Windows path from this darwin environment, but it compiles)
- [x] 6.2 Wrap every write operation (interactive save request, full `apply-plan` run) in acquire-lock / rebuild-from-disk / apply / release-lock — done in §7.1: `cmd/hypnosserve.go`'s `/api/mutate` handler and `runApplyPlan` both wrap `Import` + `ApplyOperations` + `Save` in `WithLock`
- [x] 6.3 Tests — `internal/workflowgraph/lock_test.go`: basic acquire/release and re-acquire; 20 concurrent goroutines each running a real read-sleep-write critical section under `WithLock` on the same lock path never observe more than one inside the section at once, and the resulting counter is exactly 20 (would be flaky/wrong if the lock didn't serialize); a second `Acquire` genuinely blocks until the first `Release`. Run with `-race`, clean

## 7. Local server

- [x] 7.1 Add `cmd/hypnosserve.go`: `dreamland hypnos-serve --mode=view|interactive|apply-plan`, binds `127.0.0.1:0` (ephemeral port) for view/interactive, following the `cmd/serve.go` cobra-subcommand pattern. Also completes §1.9 (importer now runs at server start via `workflowgraph.Import` and is re-triggered by the watcher's `OnChange`) and §3.8/§6.2 (the `/api/mutate` handler and `runApplyPlan` both wrap their mutation in `WithLock(... Import ... ApplyOperations ... Save ...)` — acquire, rebuild-from-disk, apply, release, every time), since none of those could be finished without a real entrypoint calling them together
- [x] 7.2 Mount read-only graph routes — `GET /api/graph` (current graph JSON), `GET /api/events` (SSE refresh stream), and `GET /api/status` (task/change status — added later, see 12.5) in both modes
- [x] 7.3 Mount write routes only when `--mode=interactive` — a single `POST /api/mutate` accepting a JSON `[]Operation` array (the same operation schema §8's plan file uses — built as one shared primitive, `internal/workflowgraph/operation.go`, rather than a separate schema per surface), not a route per operation type. Verified: view-mode has no `/api/mutate` route at all (404, not a rejected-but-present route)
- [x] 7.4 Serve embedded static assets via `go:embed` — `internal/workflowgraph/staticassets.go`. **Scope note**: this embeds a placeholder page (`static/index.html`), not the real litegraph.js UI — that's §9, explicitly deferred past this backend-focused pass; the embedding/serving mechanism itself is real and tested
- [x] 7.5 Print the local URL and open it in the default browser on start — `openBrowser` (darwin/windows/linux `exec.Command` dispatch), best-effort, printed URL is the fallback if it fails
- [x] 7.6 Tests — `cmd/hypnosserve_test.go`, all against `newHypnosMux` via `httptest.NewServer` (real HTTP, real file I/O, no real port/browser): view-mode 404s `/api/mutate`; `/api/graph` returns the actual current graph; interactive-mode mutate applies, persists to disk and to the graph cache, and updates the in-memory `guardedGraph`; an invalid operation reference 400s before any write; the SSE route streams a `data: refresh` line after `Publish`. Also covers `runApplyPlan` end-to-end from a real plan file on disk (applies, reports `applied N/M`, stops at the first runtime error with the correct partial count) — this satisfies most of §8's test list too, built here since it needed this same server-adjacent test harness. Full suite plus `-race` clean; skipped an actual `dreamland hypnos-serve` binary launch for this smoke-check specifically to avoid popping a real browser tab on the developer's machine — `openBrowser`'s effect is untested end-to-end for that reason, everything upstream of it is

## 8. Plan-driven headless apply (hypnos) and Janus dispatch

- [x] 8.1 Define the plan file format — `Operation` in `internal/workflowgraph/operation.go`, built alongside §7 as one shared primitive for both the HTTP write route and this CLI mode (see §7.3's note)
- [x] 8.2 Add `--mode=apply-plan --plan <file>` to `cmd/hypnosserve.go` — `runApplyPlan`: acquires the lock, rebuilds the graph from current disk state, validates+applies via `ApplyOperations`, releases the lock, reports `applied N/M`, never calls `net.Listen`
- [x] 8.3 Reject the whole plan before any write if an operation references a node id that doesn't exist and isn't created earlier in the same plan — `ValidateOperations`, run before any `applyOne` call
- [x] 8.4 Update `hypnos`'s own instructions in `internal/scaffold/templates/agents/*/hypnos.*` (all six platforms) — added item 7 to each file's numbered responsibility list, stating the plan-apply responsibility and the Janus in-place-of-`morpheus` dispatch rule
- [x] 8.5 Update `janus`'s own instructions in `internal/scaffold/templates/agents/*/janus.*` (all six platforms) — widened the existing "hypnos when the task creates a new agent, mengpo when it retires one" sentence to also cover "describes a workflow-graph-structural change (a routing-edge change, or a hook/skill attach/detach)"
- [x] 8.6 Tests: `TestApplyOperationsProducesSameResultAsManualEdits` (byte-identical file comparison, §"Graph-driven scaffold writer integration" tests) covers "same end state as manual edits"; `runApplyPlan` structurally never calls `net.Listen`, and `TestRunApplyPlan*` (`cmd/hypnosserve_test.go`) covers apply/reject/partial-count; `TestValidateOperationsRejectsUnknownReferenceBeforeAnyWrite` covers the invalid-reference case; `internal/workflowgraph/lock_test.go`'s `TestLockSerializesConcurrentWriters`/`TestLockBlocksUntilReleased` cover concurrent-run serialization at the lock-primitive level (a literal overlapping `hypnos-serve` process + `--mode=apply-plan` run isn't separately spawned as two OS processes in a test, but the lock mechanism both paths share is verified directly); new `internal/scaffold/hypnos_litegraph_test.go`'s `TestJanusRoutesWorkflowGraphStructuralTasksToHypnos` installs all six platforms for real and asserts both the widened `janus.*` dispatch text and the `hypnos.*` plan-apply/dispatch text landed correctly. All green, full suite + `-race` clean

## 9. litegraph.js UI

- [x] 9.1 Vendor `litegraph.js` as an embedded static asset — fetched the real MIT-licensed `litegraph.js@0.7.18` from npm (`npm pack`, not hand-written), vendored `litegraph.min.js`/`litegraph.css`/its `LICENSE` under `internal/workflowgraph/static/vendor/`. Verified it's the real thing, not a stub: served-asset test asserts >100KB and contains `"registerNodeType"`. No CDN reference anywhere
- [x] 9.2 Render the four node types and their edges from the server's graph endpoint — `static/app.js`'s `buildGraph`, using the growable-slot-aware `connectGrowable` helper for all three edge kinds
- [x] 9.3 Interactive mode controls — **scope decision, not the original 1:1 plan**: edge *creation* uses litegraph's native drag-to-connect (its best-tested core mechanic, headlessly verified in §2.4). Edge *deletion*, node create/delete, and position save use explicit toolbar buttons with plain `prompt()`/`confirm()` dialogs instead of litegraph's generic canvas search-box/keyboard-delete gestures — made deliberately, because those generic affordances are harder to get exactly right without a browser to watch them in, and a wrong assumption there fails silently. Every successful mutation triggers a full re-fetch-and-rebuild rather than an incremental client-side patch, consistent with "the server is the source of truth" throughout this change
- [x] 9.4 Surface hook scope in the UI — no separate widget: the "Attach Hook" toolbar dialog's target prompt (`"project"` or an agent id) *is* the scope choice, matching the design decision exactly
- [x] 9.5 Subscribe to the SSE refresh endpoint and re-render — `app.js`'s `subscribeEvents`, debounced/coalesced (150ms) so a burst of watcher-triggered events collapses into one rebuild; the watcher→SSE half of this path is verified for real (`cmd/hypnosserve_test.go`'s `TestWatcherToSSEIntegration` — a genuine hand-edit triggers the watcher, which publishes, which an SSE client actually receives), the browser-side `EventSource`/rebuild half is not executable outside a browser
- [x] 9.6 Visual flag for unresolved routing — `AgentNode`'s color/`boxcolor` switch based on `properties.unresolvedRouting`
- [x] 9.7 Visual distinction for `owner: external` vs. `owner: dreamland` skill nodes — `SkillNode`'s color/`boxcolor` switch based on `properties.owner`

**What could and couldn't be verified without a browser (stated plainly, not glossed over)**: The highest-risk logic — node registration, typed-slot enforcement, the dynamic multi-input pattern, and the wiring that turns a connection into an `onEdgeChanged` call with correct source/target resolution — was executed for real against the actual vendored library via Node.js (litegraph.js's own supported headless mode), not just read and assumed correct. What remains genuinely unverified: visual rendering/layout, mouse-driven drag gestures end to end, and the `prompt()`/`confirm()` dialogs' actual on-screen behavior. `go build`/`go vet`/`node --check` are clean on every file; JS syntax and the Go↔JS `Operation` field-name contract were cross-checked by hand against the real struct tags.

## 10. Slash commands

- [x] 10.1 Add `/hypnos-view` template for all six platforms — `internal/scaffold/templates/commands/{claude-code,cursor,kiro,antigravity}/hypnos-view.md`, `github-copilot/hypnos-view.prompt.md`, `codex/hypnos-view/SKILL.md`; invokes `dreamland hypnos-serve --mode=view`. Unprefixed (no `drmlnd-`) — a third command category alongside per-agent-routing and `/opsx:*`, not a Janus-routing command
- [x] 10.2 Add `/hypnos-interactive` template for all six platforms, same layout pattern; invokes `dreamland hypnos-serve --mode=interactive`
- [x] 10.3 Register both commands in the scaffold installer — **no new Go code needed**: `internal/scaffold/embed.go`'s `//go:embed all:templates` picks up new files automatically, and `installFlatCommands`/`installSkills`/`installBareCommands` already walk every file in each platform's template directory generically. On Claude Code specifically, dropping the files into `commands/claude-code/` gets them installed twice — bare at `.claude/commands/hypnos-view.md` (the wanted unprefixed path, via the existing default case in `bareCommandTargetNames`) and namespaced at `.claude/commands/drmlnd/hypnos-view.md` — a harmless redundant alias, not worth new code to suppress (see 10.4's test documenting it as intentional)
- [x] 10.4 Tests — `internal/scaffold/hypnos_litegraph_test.go`: `TestHypnosServeCommandsInstalled` confirms both commands land at the expected unprefixed path on all six platforms with the correct `--mode` flag; `TestHypnosServeCommandsAlsoReachableViaDrmlndNamespaceOnClaudeCode` locks in the redundant-alias behavior from 10.3 as intentional. All existing scaffold tests still pass (no exact-file-count assertions broken by the two new files); full suite + `-race` clean

## 11. Self-hosting bootstrap (validation instance, not the feature's primary target)

- [x] 11.1 Run the live-rebuild importer against this repository's actual installed files, confirming it produces a correct graph without needing a committed cache — ran for real (temporary test, removed after): 10 agents, correct project-scoped hooks from `.claude/settings.json`, 7 routing edges resolved from real instruction-body prose
- [x] 11.2 Hand-resolve any routing edges the importer flagged as unresolved — **finding, not a fix needed**: `janus`, `mengpo`, and `iktomi` are flagged, and correctly so. Read all three live files: each is genuinely broad-routing by design ("dispatch directly to any other agent when your own work clearly points there") with no small fixed `routes_to` set — there is no canonical single-target sentence to rewrite their prose into, and forcing one would misrepresent their actual capability. This is the importer working as designed ("flagged, not guessed"), not a defect; no file edits made
- [x] 11.3 Update this repository's own live `hypnos` and `janus` agent files to match the template changes in 8.4 and 8.5 — `.claude/agents/hypnos.md` and `.claude/agents/janus.md` (the only platform actually installed in this repo) updated identically to the corresponding templates

## 12. Validation

- [x] 12.1 Covered at the HTTP-mutation-route level, **not a literal browser drag gesture** (no browser available): `TestInteractiveModeMutateRouteAppliesAndPersists` + `TestRoutingEdgeRoundTripsThroughImport` exercise the exact same `POST /api/mutate` → `ApplyOperations` → six-platform-write path a real drag-connect triggers (verified in §2.4 to fire with the correct operation shape); only the mouse-driven UI trigger itself is unverified
- [x] 12.2 Same caveat as 12.1 — `TestCreateAgentWritesClaudeCodeFileWithHookBaseline` + the mutate-route tests cover tool-tier assignment and hook baseline through the real writer path a "Create Agent" dialog submission would invoke
- [x] 12.3 `TestApplyOperationsCreateSkillNode` (new `SKILL.md` written, `owner: dreamland`) + `TestCreateSkillWritesFileAndOwnerDreamland`; "no existing `openspec-*` file touched" holds by construction — `CreateSkill` only ever writes to the new skill's own directory (`installedSkillPlatforms` + a single `os.WriteFile` scoped to that one path), confirmed by code inspection, not a targeted test of every existing file's untouched-ness
- [x] 12.4 Covered up to the SSE-delivery boundary, same browser caveat as 9.5: `TestWatcherToSSEIntegration` makes a real hand-edit, confirms the real `Watcher` fires and a real SSE client receives the resulting message; the browser tab's own `EventSource`-triggered rebuild is unverified
- [x] 12.5 Built, with the scope correction documented in design.md's new "Task/change status overlay" decision and the corresponding spec requirement rewrite: `GET /api/status` (`cmd/status.go`) returns `{currentAgent, changes[]}` — `currentAgent` read from `.dreamland-session.json` (real session data, not fabricated), `changes` from `openspec list --json` (real CLI output, parsed), both tolerating absence/failure gracefully (empty, not an error). `app.js`'s `loadStatus`/`applyStatus` fetch it alongside every graph load (initial + every SSE-triggered refresh), highlight the current agent's node, and render a change-progress panel. Tests: `cmd/status_test.go` (session-file present/missing/malformed, `openspec` JSON parsing including malformed/empty-key cases, missing-binary tolerance via `PATH=""`, both sources assembled together) and `cmd/hypnosserve_test.go`'s `TestAPIStatusRoute` (real HTTP round trip). All green, full suite + `-race` clean. Same browser caveat as 9.5/12.4: the highlight/panel *rendering* isn't executable outside a browser, but the full data path up to the browser boundary is verified for real, not assumed
- [x] 12.6 `TestApplyOperationsProducesSameResultAsManualEdits` (byte-identical file comparison between a plan and equivalent manual calls) + `TestRunApplyPlanAppliesFromRealFile` (the real CLI path, real file on disk)
- [x] 12.7 Covered at the lock-primitive level, not two separately-spawned OS processes: `TestLockSerializesConcurrentWriters` (20 concurrent goroutines through `WithLock` never observe more than one in the critical section, correct final state) proves the mechanism both the interactive-save and `apply-plan` code paths share is genuinely exclusive; a literal two-process `hypnos-serve`+`apply-plan` race wasn't separately spawned, same as noted in 8.6
