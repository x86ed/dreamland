Dispatch notes. Go code (`cmd/`, `internal/routing/`, `internal/sessionidentity/`, `internal/oneiroi/`, `internal/scaffold/*.go`, `internal/config/`) is `nyx` (tests first) then `morpheus`. `internal/scaffold/templates/hooks/bindings/**` (including `settings-patch.json`) is not under a protected path and is `morpheus` work. Anything under `internal/scaffold/templates/agents/**` or `.../commands/**` is writable only by `hypnos` (fixed artifact ownership), so those tasks are tagged `hypnos` even where they are prose edits. Each task line ends with the `[flow: ...]` tag this change introduces (see the `deterministic-routing` spec), so this file also exercises the convention.

## 0. Live verification of platform assumptions (gate for groups 2, 3, 5, and 10)

Run against a real Claude Code session in a scratch repo with a build of `dreamland` from this branch. Record observed payloads and results under this task in the commit message of the task that follows; if an assumption fails, stop and return to `phantasos` to amend design.md (do not work around it silently).

- [ ] 0.1 Add a temporary `PreToolUse` hook that dumps stdin to a scratch file, then observe: (a) under `claude --agent janus`, does the payload carry top-level `agent_type: "janus"`; (b) under plain `claude`, is `agent_type` absent; (c) does a frontmatter `hooks.PreToolUse` entry in `janus.md` fire under `--agent janus`. Outcome decides which of the two guard wirings is load-bearing (design.md, Risks). [flow: morpheus]
- [ ] 0.2 With `tools: Read, Bash, Agent(morpheus, nyx)` on a scratch agent run as `--agent`, confirm it can spawn `morpheus`, cannot spawn `general-purpose`, and that `Agent` appears as the `tool_name` in `PreToolUse`. [flow: morpheus]
- [ ] 0.3 Confirm the `Bash` `PreToolUse` payload shape: `tool_name` `Bash`, command at `tool_input.command`; and whether a `PowerShell` tool exists on the installed version and its `tool_name`/`tool_input` field. `guard-router` field names in group 2 are taken from this observation. [flow: morpheus]
- [ ] 0.4 Try `!`-prefixed inline command execution (with `allowed-tools: Bash(dreamland route:*)`) in a scratch slash command with no interpolated user text; confirm the output is in the model's prompt before its first turn. Adopt in group 5 only if it works on the installed version. [flow: morpheus]
- [ ] 0.5 Baseline: run the same three requests (free-form, `/opsx:apply` on a one-change repo, `/drmlnd:morpheus`) on the pre-change build and record `dreamland telemetry` token totals and number of subagent turns, so group 10 can compare after. [flow: morpheus]
- [ ] 0.6 **Hard gate for the default-block policy.** Because an unattributed session is now blocked, a specialist whose payload lacks identity would be blocked too. With a temporary `PreToolUse` dump hook, dispatch each of the nine built-in agents, one seeded agent, the built-in `Explore`, and two parallel/background dispatches, have each perform an `Edit` and a `Bash` call, and record whether the payload has a top-level `agent_type` (and whether a top-level `agent_id` is also present, and whether it appears without `agent_type` in any case). Also record the payload for a `claude --agent morpheus` main session. If any dispatched subagent's `Edit`/`Bash` payload lacks `agent_type` and also lacks `agent_id`, stop: the default reverts to allow and this change returns to `phantasos` (design.md, Risks). If `agent_id` does not exist on the installed version, strike the `agent_id` resolution step and its scenario from the `router-dispatch-guard` spec. [flow: morpheus]
- [ ] 0.7 Confirm `session_id` is a string present at top level of the `SessionStart`, `PreToolUse` (`Agent`), `Stop`, and `SubagentStop` payloads; is identical across those events within one session; differs between two parallel windows in one repository; and satisfies `^[A-Za-z0-9._-]{1,128}$` (record an actual sample). Confirm the `Stop` hook command receives the payload on stdin when the command is `dreamland test-and-commit ... --hook`. Also record the `Stop` payload's `agent_type` under `claude --agent janus` (the design ignores it for `turn-complete`, so this is informational). [flow: morpheus]
- [ ] 0.8 Confirm that an `Agent` call with `subagent_type` omitted reports no `tool_input.subagent_type`, and the exact spellings the field takes for `Explore` and `Plan`. `guard-router`'s `Agent` check uses these. [flow: morpheus]
- [ ] 0.9 Gate for group 10b (Decision 10). (a) On the installed Go toolchain (and once on Windows), confirm `go build -o <out> -ldflags=-X=dreamland/cmd.buildCommit=<sha> .` embeds the commit (`<out> version` prints it); it was confirmed on macOS while drafting, so only Windows is outstanding. If the space-free `-X=` form fails there, return to `phantasos` (the placeholder grammar would need a structured argv form). (b) Confirm a `permissions.allow` entry `Bash(dreamland build)` matches the bare command and does not match `dreamland build x`. (c) On Windows, confirm renaming a running `dreamland.exe` to `dreamland.exe.old` and renaming a new file into its place succeeds, and that `os.Rename` over the running file alone fails, as design.md assumes. [flow: morpheus]

## 1. `dreamland route`

- [ ] 1.1 Write `internal/routing/routing_test.go` covering every scenario in the `deterministic-routing` spec against a fake `Env` (no real `openspec`): rules 1-6 in order, state resolver outcomes, tag parsing, unregistered `--to`/tag exits with an error, `janus` rejected, seeded agent accepted, roster populated on ambiguity, rule ids stable. [flow: nyx]
- [ ] 1.2 Create `internal/routing/routing.go`: `type Input struct{ To, Command, Change, Request string }`, `type Result struct` with the JSON fields in the spec (`decision`, `target`, `rule`, `reason`, `change`, `task`, `candidates`, `ask_user`, `roster`, `next`), `type Env interface{ ListChanges() ([]Change, error); ChangeStatus(name string) (Status, error); ReadTasks(name string) (string, error); Registered() map[string]bool; Roster() []RosterEntry }`, and `func Resolve(in Input, env Env) (Result, error)` implementing rules 1-6 and the state resolver. Intent phrases live in one package-level table. Tag parsing is `\[flow:\s*(nyx|morpheus|hypnos|mengpo)\]` on the first unchecked `- [ ]` line. [flow: morpheus]
- [ ] 1.3 Create `cmd/route.go` (`routeCmd`, flags `--to`, `--command`, `--change`, `--stdin`; trailing args or stdin as request): builds an `Env` backed by `exec.LookPath("openspec")` + `openspec list --json` / `openspec status --change <n> --json` (no shell), reads `openspec/changes/<name>/tasks.md` with `filepath`, `agentidentity.Registered(repoRoot)` for targets and `.dreamland/oneiroi/registry.json` roles for `roster`; prints one JSON object; errors return a plain error (exit 1), never `Blocking`. Writes no files. [flow: morpheus]
- [ ] 1.4 Add `cmd/route_test.go` asserting no side effects (`git status --porcelain` and `.dreamland-session.json` unchanged), exit 1 when `openspec` is missing, and stdin request literalness. Add a Windows build/test in the style of `cmd/otel_receiver_windows_build_test.go` asserting backslash paths resolve. [flow: nyx]
- [ ] 1.5 Add the drift test from the `deterministic-routing` spec (rule targets and `--command` spellings versus the routing table in every platform's `janus.*` template; flow-tag set versus the `/opsx:apply` entry). It fails until group 5 lands the templates; keep it in the tree red-then-green. [flow: nyx]

## 2. `dreamland guard-router`

- [ ] 2.1 Write `cmd/guard_router_test.go` from every scenario in the `router-dispatch-guard` spec, table-driven for: the Bash allowlist (allowed forms; each blocked metacharacter including `'` and `"`; `--output=`, `-o`, `--ext-diff`; a quoted `"--output=x"`; quoted-heredoc allowed / continued-after-terminator / on-another-command; `PowerShell` always blocked); the `go vet`/`go test` grammar (allowed: `./...`, `-race`, `-count=1`, `-run=TestFoo`; blocked: `-o`, `-c`, `-exec`, `-toolexec`, `-vettool`, `-overlay`, `-modfile`, `-coverprofile`, `-cpuprofile`, `-fuzz`, `-args`, `-C`, `../x`, absolute paths, a separate-token value like `-run TestFoo`); the delegated set (`go build ./...`, `go build -o x .`, `mv a b`, `cp a b`, `git commit`, `git branch x`, `openspec new`, `dreamland commit`) each exits 2 with the message described below; identity resolution (flag beats payload; payload beats `agent_id`; `agent_id` without `agent_type` exits 0; neither present is unattributed and treated as `janus`; git config never consulted (set `user.name` to `janus` and assert no effect); a per-session identity file holding `morpheus` never consulted); `dreamland build` allowed only as exactly those two tokens from both `janus` and an unattributed session, and blocked with any argument, flag, or trailing shell text; the block message for `go build`/`go install`/`mv`/`cp` names `dreamland build` when `.dreamland.json` has a `build_command` and names `dreamland init` when it does not (both name `morpheus` for anything else); the policy (absent/`"block"`/unknown blocks, unknown notes on stderr; `"allow"` exits 0 for unattributed only and never for `agent_type` `janus`); the `Agent` check (registered allowed, seeded allowed, `Explore`/`Plan` allowed, `general-purpose`/missing/`janus` blocked, a specialist's own `Agent` call exits 0). Stub exit via a `guardRouterExit` variable as `guard_artifact.go` does. [flow: nyx]
- [ ] 2.2 Add `MainThreadGuard string \`json:"main_thread_guard,omitempty"\`` to `internal/config/config.go` `Config`; recognised values are `""` (default, means block), `"block"`, and `"allow"`; any other value is kept as-is and treated as `block` with a stderr note by the guard. Test in `internal/config/config_test.go`. [flow: nyx]
- [ ] 2.3 Create `cmd/guard_router.go`: `guardRouterCmd` (`--agent-name`), `resolveActingIdentity(flag string, payload guardRouterPayload) (identity string, attributed bool)`, `janusBashAllowed(command string) (ok bool, reason string)` (including the go grammar, the heredoc exception, and `dreamland build` as exactly two tokens), `janusAgentAllowed(subagentType, repoRoot string) (ok bool, reason string)` using `agentidentity.IsRegistered` (excluding `janus`) plus `Explore`/`Plan`, and the blocking message builder (reuse `artifactOwners` from `cmd/guard_artifact.go` to name the owner for protected paths; otherwise say the session is acting as the janus orchestrator, point at `dreamland route`, name `iktomi` as the free-form default, `dreamland build` (or `dreamland init` when `.dreamland.json` has no `build_command`, read with the same loader as the policy) for building and installing, and `morpheus` for other build/install/git-mutation work; for an unattributed session add the user-only `.dreamland.json` opt-out). Deny via `Blocking(...)` so `Execute()` exits 2; empty/malformed payload or no repository returns nil. Field names per tasks 0.3, 0.6, 0.8. [flow: morpheus]
- [ ] 2.4 Extend the payload struct (or add one) to include `tool_name`, `tool_input.command`, `tool_input.subagent_type`, `agent_id`, and `session_id`; keep `guard-artifact` behavior byte-identical (its tests must still pass unchanged). [flow: morpheus]

## 3. Scaffold plumbing (Go)

- [ ] 3.1 Write `internal/scaffold/scaffold_test.go` cases: rendered Claude Code `janus.md` has `tools: Read, Bash, Agent(<nine built-ins, then registry entries in order>)` and no `janus`; re-render with a seeded registry entry keeps it; `settings.json` merge yields `guard-router` exactly once and preserves user `PreToolUse` entries; no top-level `"agent"` key; the guard matcher is `Write|Edit|MultiEdit|NotebookEdit|Bash|PowerShell|Agent|Task`; `permissions.allow` contains `Bash(dreamland route *)` and `Bash(dreamland build)` and neither is duplicated on repeat init; `dreamland init` prints a one-line note (guard added, how to opt out) only when it newly adds the `guard-router` binding. [flow: nyx]
- [ ] 3.2 In `internal/scaffold/scaffold.go`, render a `{{DISPATCH_TARGETS}}` placeholder in the Claude Code Janus template from `agentidentity.Registered(cfg.RepoRoot)` (built-ins in fixed order, then registry order, never `janus`). Non-Claude templates are not rendered. [flow: morpheus]
- [ ] 3.3 Extend the `settings.json` merge to union-merge `permissions.allow` from the patch (array union, no duplicates) if `mergeJSON` currently overwrites arrays; add `"permissions": {"allow": ["Bash(dreamland route *)"]}` handling accordingly. [flow: morpheus]
- [ ] 3.4 Implement the one-line init note from 3.1 (printed to stdout by `dreamland init` when the `guard-router` entry was not previously present in `.claude/settings.json`). [flow: morpheus]

## 4. Roster coherence

- [ ] 4.1 Write tests in `internal/oneiroi/routing_test.go`: `seed`/`fork` add the new name to the installed `janus.md` `Agent(...)` list in registry order, are idempotent, never add `janus`, and no-op when the file is absent; a helper `RemoveDispatchTarget` removes a name and is idempotent. [flow: nyx]
- [ ] 4.2 In `internal/oneiroi/routing.go` add `AddDispatchTarget(repoRoot, agentName string) (touched bool, err error)` and `RemoveDispatchTarget(repoRoot, agentName string) (touched bool, err error)` operating on the `Agent(...)` group in the installed Claude Code Janus file's `tools:` line; call `AddDispatchTarget` from the same place `AddStubEdges` is called in `cmd/oneiroi.go` (seed/fork), and include the file in that command's commit path list. [flow: morpheus]
- [ ] 4.3 Add the registry-versus-`Agent(...)` drift test from the `agent-lifecycle-management` spec (fails when a registered name, excluding `janus`, is missing from the list or the list contains an unregistered name). [flow: nyx]
- [ ] 4.4 Check whether the existing routing-table stub edge survives a forced re-init; record the answer in design.md's Decision 6. If it does not, open a follow-up rather than widening this change. [flow: morpheus]
- [ ] 4.5 Update `internal/scaffold/templates/agents/claude-code/mengpo.md`'s archive and hard-delete steps to also remove the agent's name from the `Agent(...)` list in the installed `.claude/agents/janus.md` (`mengpo` has `Write`; there is no `oneiroi` retire subcommand today and this task does not add one). The registry-versus-`Agent(...)` drift test (4.3) is the backstop if the step is missed. `RemoveDispatchTarget` (4.2) is provided for tests and any future command. [flow: hypnos]

## 5. Templates and command files (`hypnos`)

- [ ] 5.1 `internal/scaffold/templates/agents/claude-code/janus.md`: set `tools: Read, Bash, Agent({{DISPATCH_TARGETS}})`; replace the `hooks.Stop` block with `hooks.PreToolUse` (matcher `Write|Edit|MultiEdit|NotebookEdit|Bash|PowerShell|Agent|Task`, command `dreamland guard-router --agent-name janus`); rewrite the opening protocol to at most one short paragraph deferring to `dreamland route` (run it with the request, dispatch `target`, decide among `candidates` or ask the user when `ask_user`, never do the work); keep the routing table, the hand-off validation paragraph from `claude-code-parity`'s `janus-dispatch-guardrails`, and the verbatim-forwarding rule; add `dreamland route` and the `[flow: ...]` task tag names (including legacy `openspec-*` spellings) to the table so the drift test passes; state that free text is passed with `dreamland route --stdin` in a quoted heredoc with delimiter `DREAMLAND_ROUTE_EOF`. [flow: hypnos]
- [ ] 5.2 `internal/scaffold/templates/commands/claude-code/route.md`: replace "Delegate this request to the `janus` agent" with instructions to run `dreamland route` (free text via quoted heredoc on `--stdin`; inline pre-exec form only if task 0.4 passed) and dispatch the returned `target` with the `Agent` tool, forwarding the request verbatim; on `ambiguous`, choose among `candidates` or ask the user if `ask_user`. Keep the `<!-- dreamland-managed -->` marker handling identical (bare `dreamland.md`/`janus.md` copies derive from this file). [flow: hypnos]
- [ ] 5.3 `internal/scaffold/templates/commands/claude-code/{baku,hypnos,iktomi,mengpo,morpheus,nyx,phantasos,phobetor,zhougong}.md`: replace the "delegate to the `janus` agent with an explicit instruction" text with "dispatch `<agent>` directly with the `Agent` tool, forwarding the request verbatim; do not spawn `janus`". Update the generator in `internal/scaffold/stub.go` (line ~226, `body := fmt.Sprintf("Delegate this request to the `janus` agent ...`) for Claude Code so `dreamland oneiroi seed`/`fork` produce the same text. [flow: hypnos]
- [ ] 5.4 `internal/scaffold/templates/hooks/bindings/claude-code/settings-patch.json` (not a protected path, so `morpheus`, not `hypnos`): add a `PreToolUse` entry, matcher `Write|Edit|MultiEdit|NotebookEdit|Bash|PowerShell|Agent|Task`, command `dreamland guard-router`; add `permissions.allow` `Bash(dreamland route *)` and `Bash(dreamland build)` (exact, no wildcard). Leave every other entry unchanged (the `Stop` `--hook` change is task 10.6). [flow: morpheus]
- [ ] 5.5 `phantasos.*` in all six platform template trees: add one instruction that every checkbox task line in `tasks.md` ends with exactly one of `[flow: nyx]`, `[flow: morpheus]`, `[flow: hypnos]`, `[flow: mengpo]`, with the one-line meaning of each. [flow: hypnos]
- [ ] 5.6 Update the `/opsx:apply` command template (the generated `.claude/commands/opsx/apply.md` "Routing" line) and the `openspec-apply-change` stub to describe `dreamland route --command opsx:apply` for Claude Code. If these files are upstream-generated by OpenSpec rather than dreamland templates, record where they come from and make the change in the dreamland-owned source instead of the generated copy. [flow: hypnos]
- [ ] 5.7 Update `README.md` (routing section, the Janus and Hypnos rows of the agent table, and a short "launch modes" note: `claude --agent janus` vs `/dreamland`; that plain sessions are guarded as `janus` by default and how `.dreamland.json` `main_thread_guard: "allow"` opts out; that the binary is rebuilt and installed with `dreamland build` (configured by `dreamland init`; replace the `go build ... -ldflags` line in the build section with the `build_command` form and keep the ldflags form as what it runs), with the allowed main-session commands listed and ad hoc `go build`/`mv`/`cp` delegated to `morpheus`; the per-session identity record and `DREAMLAND_STATE_DIR`). [flow: morpheus]
- [ ] 5.8 `internal/scaffold/templates/agents/claude-code/janus.md`: add one line to the orchestrator protocol, next to the allowed-commands list: to rebuild and install the project binary run `dreamland build` (no arguments); never `go build`, `mv`, or `cp`; if it reports no build command is configured, tell the user to run `dreamland init`. The drift test (1.5) needs no change. [flow: hypnos]

## 6. Wiring tests

- [ ] 6.1 Extend the scaffold tests to assert, per platform, that only Claude Code's installed bindings reference `guard-router`, and that Claude Code's `janus.md` has no `hooks.Stop` while the other nine agents' blocks are byte-identical to their pre-change templates. [flow: nyx]
- [ ] 6.2 Add an end-to-end test (build the binary, feed recorded hook payloads) exercising `guard-router` under `janus` and under an unattributed session for Edit, Write, allowed Bash (`go test ./...`), blocked Bash (`go build ./...`), `Agent` to a registered agent, and `Agent` to `general-purpose`, asserting exit codes 2/2/0/2/0/2. [flow: nyx]

## 7. Live verification (after groups 1-6)

- [ ] 7.1 In a scratch repo: `claude --agent janus`, ask for a code change; confirm Janus runs `dreamland route`, dispatches `iktomi`, and any attempted `sed -i` or `Edit` by Janus is blocked with the dispatch message. [flow: morpheus]
- [ ] 7.2 In the same repo: plain `claude`, `/dreamland <request>`; confirm no `janus` subagent appears in the transcript and the routed agent runs. Repeat for `/opsx:apply` with a tagged task and an untagged task, and for `/drmlnd:morpheus`. [flow: morpheus]
- [ ] 7.3 Two parallel windows in the same repo, one `--agent janus`, one plain `claude`; confirm the guard blocks both from editing (the plain one as unattributed `janus`) and neither is affected by `git config user.name` contents; set `main_thread_guard` to `"allow"` and confirm only the plain window is released while the `--agent janus` window stays blocked. [flow: morpheus]
- [ ] 7.4 Repeat the 0.5 requests on the new build and record token totals and subagent-turn counts next to the baseline; if the routing saving is not visible, say so in the change's archive notes. [flow: morpheus]
- [ ] 7.5 Plain `claude`, default config: attempt an `Edit`, then `go build -o dreamland .` and `mv` to install, then `go vet ./...` and `go test ./...`, then an `Agent` call to `general-purpose`, then `dreamland build`. Confirm the first, second, and fifth-to-last...  [flow: morpheus]

## 8. Self-heal of existing installs

- [ ] 8.1 Add a staleness rule to `internal/scaffold/scaffold.go` so a plain `dreamland init` (no `Force`) rewrites (a) `.claude/agents/janus.md` when its `tools:` line lacks `Agent(`, and (b) prefixed and bare Claude Code command files still containing the sentinel text "Delegate this request to the `janus` agent", following the `installFlatCommands` `commandName` staleness precedent. A user-edited `janus.md` that already has `Agent(` is left alone. Tests for both, including an untouched already-current install. [flow: nyx]
- [ ] 8.2 Implement 8.1's rule (`isStaleJanus`, `isStaleRouteCommand`), keeping the check content-based and narrow. [flow: morpheus]

## 9. Other platforms (included by user decision; still separable, so cutting it does not affect the Claude Code fix)

- [ ] 9.1 Add one line to `janus.*` for GitHub Copilot, Codex CLI, Cursor, Kiro, and Antigravity: run `dreamland route --stdin` (or with the request) first and follow `decision`/`target`; on `ambiguous`, decide among `candidates` yourself. No hook, tool, or `agents:` graph change. Confirm the drift test (1.5) covers all six trees. [flow: hypnos]

## 10. Per-session Stop identity (Claude Code; design.md Decision 7)

Sequencing: start after group 0 (0.7 in particular) and after groups 1-9 are merged or at least do not share a file in flight. These tasks edit files that `harden-commit-hook-enforcement` already rewrote (`cmd/coauthor.go`, `cmd/commit.go`, `cmd/testandcommit.go`, `settings-patch.json`); all of that change's implementation tasks (groups 1-9 and 11) are complete and in the tree, so this builds on the current tree with no merge to resolve. Its open tasks 10.1 and 10.2 are evidence/bookkeeping for `claude-code-self-hosting`, touch no code or spec text, and do not block anything here; they matter only to the archive order in task 11.4.

- [ ] 10.1 Write `internal/sessionidentity/sessionidentity_test.go`: `Root()` honors `DREAMLAND_STATE_DIR`, else `<UserCacheDir>/dreamland`, else `<TempDir>/dreamland`; with `DREAMLAND_STATE_DIR` set it is the same directory `otelreceiver.StateDir()` returns (a test-only import), and `identity/` collides with none of the receiver's `sessions`, `receiver.log`, `receiver-<port>.json|lock` names; `ValidSessionID` accepts `[A-Za-z0-9._-]{1,128}` and rejects `.`, `..`, `../x`, empty, 129 chars, and `:`/`\` (Windows); `Write` is atomic (a concurrent reader never sees partial JSON: run writer and reader goroutines under `-race`), replaces an existing file, creates the directory 0700 and file 0600 (skip the mode assertion on Windows); `Read` returns `("", false)` for missing, unreadable, or unregistered content; the prune pass removes entries with mtime older than 7 days and ignores errors. Add a Windows-path build/test in the style of `cmd/otel_receiver_windows_build_test.go`. [flow: nyx]
- [ ] 10.2 Create `internal/sessionidentity/sessionidentity.go`: `Root() string`, `ValidSessionID(id string) bool`, `Write(sessionID, agent string) error` (temp file in the same directory, `os.Rename`, prune best effort), `Read(sessionID, repoRoot string) (agent string, ok bool)` applying `agentidentity.IsRegistered` (unregistered means not ok). No dependency on `cmd` or on `otelreceiver`. [flow: morpheus]
- [ ] 10.3 Extend `cmd/coauthor_test.go`: with `--hook` and a payload carrying a valid `session_id`, `SessionStart` records `janus` and `PreToolUse` `Agent` with `subagent_type` `nyx` records `nyx`, and `git config --local user.name` is still set; an invalid `session_id` writes nothing; `--agent-name` mode never writes a record; an unwritable state directory (a file in place of the directory) still sets git config, prints an advisory note, and exits 0; two temp state roots via `DREAMLAND_STATE_DIR` isolate tests. [flow: nyx]
- [ ] 10.4 In `cmd/coauthor.go` `runCoauthor`, when `--hook` is set and the payload has a valid `session_id`, call `sessionidentity.Write` with the resolved (registered) agent name in the same step as the git config writes; a write error is advisory. Keep the git config writes and the `Blocking` behavior for them unchanged. [flow: morpheus]
- [ ] 10.5 Extend `cmd/commit_test.go` and `cmd/testandcommit_test.go` from the `dev-workflow-hooks` scenarios: `commit --reason turn-complete --hook` with `session_id` `S1` recording `nyx` while another session recorded `morpheus` and set git config to `morpheus` commits as `nyx` (subject and author); the payload's `agent_type: janus` does not override the record; a valid `session_id` with no record commits as `janus` even when git config holds another name; no `--hook` or no valid `session_id` still uses git config; handoff still prefers the payload's `agent_type`; an unregistered recorded value reads as `janus`; `test-and-commit --hook` reads stdin once (feed a pipe that errors on a second read) and forwards the payload to the commit step; the handoff non-blocking behavior and the pinned `-c user.name` author are unchanged. [flow: nyx]
- [ ] 10.6 Implement 10.5: in `cmd/commit.go` replace the `currentGitIdentityName` step with the resolution chain in the MODIFIED requirement's item 4 (`--agent-name`; handoff payload `agent_type`; session record or `janus`; git config only without a valid `session_id`; `resolveEnforcedAgentName`), read the payload once via `agentNameFromHookPayloadFrom`-style parsing extended to return `session_id`; add `--hook` to `cmd/testandcommit.go` and forward it (and the parsed payload) to `runCommit`; change the `Stop` entry in `internal/scaffold/templates/hooks/bindings/claude-code/settings-patch.json` to `dreamland test-and-commit --reason turn-complete --hook`, and extend the scaffold test that pins the `Stop` binding (task 6.1 style) to require `--hook` on Claude Code and its absence on every other platform's binding. [flow: morpheus]
- [ ] 10.7 Live check with two parallel windows in one repository (extends 7.3): window A dispatches `nyx`, window B dispatches `morpheus`, each ends a turn with a pending change; `git log --format='%an | %s'` shows `nyx` for A's commit and `morpheus` for B's, and `ls <state-root>/identity` shows two files. Repeat with `git config --local user.name` forcibly set to a third value between dispatch and Stop. [flow: morpheus]

## 11. Reconcile, re-sync, archive readiness

- [ ] 11.1 Confirm `claude-code-parity`'s amended artifacts (`specs/fixed-pipeline-enforcement/spec.md`, `specs/agent-scaffolding/spec.md`, `design.md`, `proposal.md`, `tasks.md`) agree with this change: Janus `tools` is `Read, Bash, Agent(<roster>)`, no top-level `"agent"` key, janus.md has a `PreToolUse` guard and no `hooks.Stop`, the other nine agents keep the five-command block. If `claude-code-parity` has already been archived when this runs, apply the same corrections to `openspec/specs/fixed-pipeline-enforcement/spec.md` and `openspec/specs/agent-scaffolding/spec.md` in this change's archive step instead. No archive order between these two changes is required. [flow: nyx]
- [ ] 11.2 Re-sync this repository's live files from the templates through the installer (the staleness rule from group 8, or the installer's force path if run before it lands), not by hand-editing `.claude/`; confirm `.claude/settings.json` now also carries the pre-existing `guard-artifact` binding and the new `guard-router` and `Stop --hook` entries. Coordinate with `claude-code-parity` task 9.4, which is the same re-sync, so it happens once. Before re-syncing, confirm the `dreamland` on `PATH` is the branch build (delegated to `morpheus` under the new guard; the binary must be rebuilt and installed by a dispatched specialist, not by the orchestrator session). [flow: morpheus]
- [ ] 11.3 Run `openspec validate deterministic-routing-and-janus-guard --strict`, `openspec validate claude-code-parity --strict`, and `openspec validate harden-commit-hook-enforcement --strict` and `MERGE_CHECK=1 bash scripts/pre-merge-check.sh`; `go build ./...`, `go vet ./...`, `go test ./...` (with `-race` for `internal/sessionidentity`) pass on the branch. [flow: morpheus]
- [ ] 11.4 Archive order: `harden-commit-hook-enforcement` before this change (it may be archived with its open 10.1/10.2 if the user so decides), because both MODIFY `dev-workflow-hooks`' `dreamland commit auto-commits...` requirement and this change's text is the union that must be the last writer; before archiving harden, rebase its delta for that header onto main (it is stale against `fix-claude-code-handoff-integrity`), or accept that this change's later archive overwrites it. Never archive this change first. `claude-code-parity` has no ordering constraint relative to this change. [flow: mengpo]
